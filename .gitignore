node_modules
.pnpm-store
.next
expo-env.d.ts
.env
.env.local
.DS_Store
coverage
*.log

# Artefatos de build — sao gerados, nao versionados.
# `packages/shared/dist/` e `apps/web/tsconfig.tsbuildinfo` estavam no
# indice: alem de poluir todo diff, versionar saida de compilacao cria
# a chance de o commit e o codigo-fonte discordarem.
dist
build
*.tsbuildinfo
